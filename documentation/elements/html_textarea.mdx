import { Canvas, Meta } from "@storybook/blocks";

<Meta title="HTML Elements/HTML Text Area" />

# HTML Text Area

Due to the way Boondoggle works, you are importing a global stylesheet, so will
avail of all element style rules by default.

<Canvas>
  <textarea placeholder="Type something" />
</Canvas>
