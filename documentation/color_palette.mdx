import { ColorItem, Meta } from "@storybook/blocks";

import { Box } from "../src/components/box";
import { getTheme, vars } from "../src/styles/theme.css";
import { ColorPalette } from "./color_palette.component";

<Meta title="Documentation/Color palette" />

# Color palettes

## Base color palette (light mode)

<Box className={getTheme()}>
  <ColorPalette />
</Box>

## Base color palette (dark mode)

<Box className={getTheme({ darkMode: true })}>
  <ColorPalette />
</Box>

## Intent: Good (light mode)

<Box className={getTheme({ intent: "good" })}>
  <ColorPalette>
    <ColorPalette />
  </ColorPalette>
</Box>

## Intent: Good (dark mode)

<Box className={getTheme({ darkMode: true, intent: "good" })}>
  <ColorPalette>
    <ColorPalette />
  </ColorPalette>
</Box>

## Intent: Bad (light mode)

<Box className={getTheme({ intent: "bad" })}>
  <ColorPalette>
    <ColorPalette />
  </ColorPalette>
</Box>

## Intent: Bad (dark mode)

<Box className={getTheme({ darkMode: true, intent: "bad" })}>
  <ColorPalette>
    <ColorPalette />
  </ColorPalette>
</Box>
