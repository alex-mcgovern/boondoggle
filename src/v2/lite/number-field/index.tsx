import {
	NumberField as RACNumberField,
	NumberFieldProps as RACNumberFieldProps,
} from "react-aria-components";

export const NumberField = (props: RACNumberFieldProps) => {
	return <RACNumberField {...props} />;
};
