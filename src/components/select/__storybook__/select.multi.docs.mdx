import { ArgTypes, Canvas, Meta, Story } from "@storybook/addon-docs";

import * as stories from "./select.multi.stories.tsx";

<Meta of={stories} />

# Select

- Wrapper around Downshift to provide a select component with a few options for
  styling and behavior

## Default

<Canvas name="default" of={stories.SelectMultiDefault} />

## Filterable

[ToDo: Add filterable example]
{/* <Canvas name="filterable" of={stories.Filterable} /> */}
