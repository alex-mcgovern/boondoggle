import { ArgTypes, Canvas, Meta, Story } from "@storybook/addon-docs";

import * as stories from "./input_error_message.stories.tsx";

<Meta of={stories} />

# InputErrorMessage

## Overview

`InputErrorMessage` is a simple styled component used to display an error
message for an `Input` component. (Which may be nested in other components, like
a `Form` or `Select`)

<Canvas of={stories.Default} />
<Canvas of={stories.SlotLeft} />
<Canvas of={stories.SlotRight} />
