import {
    ArgTypes,
    Canvas,
    Controls,
    Meta,
    Story,
    Source,
} from "@storybook/blocks";

import { SearchField } from ".";
import * as stories from "./stories.tsx";

<Meta of={stories} />

# SearchField

A `SearchField` is a text field designed for searches.

It is a thin layer on top of the
[React Aria SearchField component](https://react-spectrum.adobe.com/react-aria/SearchField.html).

## Example

<Canvas
    sourceState="shown"
    of={stories.Default}
/>
