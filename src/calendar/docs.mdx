import {
    ArgTypes,
    Canvas,
    Controls,
    Meta,
    Story,
    Source,
} from "@storybook/blocks";
import { Calendar } from ".";
import * as stories from "./stories.tsx";

<Meta of={stories} />

# Calendar

A calendar displays one or more date grids and allows users to select a single
date.

It is a thin layer on top of the
[React Aria Calendar component](https://react-spectrum.adobe.com/react-aria/Calendar.html)
with some additional props for styling / variants.

## Example

<Canvas
    sourceState="shown"
    of={stories.Default}
/>

## Props

<ArgTypes of={Calendar} />
