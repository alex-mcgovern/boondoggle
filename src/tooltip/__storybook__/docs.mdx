import {
    ArgTypes,
    Canvas,
    Controls,
    Description,
    Meta,
    Source,
    Story,
    Title,
} from "@storybook/blocks";

import { Tooltip } from "..";
import * as stories from "./stories.tsx";

<Meta of={stories} />
<Title of={stories} />
<Description of={Tooltip} />

## Example

<Canvas of={stories.TriggerInfoButton} />


## Positioning

The Tooltip's placement with respect to its trigger element can be adjusted
using the `placement` prop.

<Canvas of={stories.PlacementRight} />
<Controls
    of={stories.PlacementRight}
    include={["placement"]}
    meta={stories.tooltipTriggerMeta}
/>

## Props

<ArgTypes of={Tooltip} />
