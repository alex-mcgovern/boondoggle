import {
    ArgTypes,
    Canvas,
    Controls,
    Description,
    Meta,
    Source,
    Story,
    Title,
} from "@storybook/blocks";

import { Tooltip } from "..";
import * as root_stories from "./tooltip-root.stories.tsx";
import * as body_stories from "./tooltip-body.stories.tsx";
import * as info_button_stories from "./tooltip-info-button.stories.tsx";

<Meta title="Tooltip" />
<Title>Tooltip</Title>
<Description of={Tooltip} />
<Canvas of={root_stories.TriggerInfoButton} />

## Trigger

The `Tooltip` namespace provides a `InfoButton` component for the common use
case of a tooltip triggered by an info icon.

<Canvas of={root_stories.TriggerInfoButton} />

But the tooltip can also be triggered by any Button or Link.

<Canvas of={root_stories.TriggerButton} />
<Canvas of={root_stories.TriggerLink} />

## Placement

The Tooltip's placement with respect to its trigger element can be adjusted
using the `placement` prop on `Tooltip.Body`.

<Controls
    of={body_stories.Placement}
    include={["placement"]}
/>
<Canvas of={body_stories.Placement} />

## Delay


The delay can be adjusted for hover using the `delay` prop.
The delay time for the tooltip to close can be adjusted using the `closeDelay` prop.

Tooltips appear after a short delay when hovering the trigger, or
instantly when using keyboard focus. This delay is called the "warmup period",
and it is a global timer, shared by all tooltips. Once a tooltip is displayed,
other tooltips display immediately. If the user waits for the "cooldown
period" before hovering another element, the warmup timer restarts. 

[Source:
React Aria Components](https://react-spectrum.adobe.com/react-aria/Tooltip.html#delay)

<Controls
    of={root_stories.Delay}
    include={["delay", "closeDelay"]}
/>
<Canvas of={root_stories.Delay} />

## Props

### Tooltip.Root

<ArgTypes of={Tooltip.Root} />

### Tooltip.Body

<ArgTypes of={Tooltip.Body} />

### Tooltip.InfoButton

<ArgTypes of={Tooltip.InfoButton} />
