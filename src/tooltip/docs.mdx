import {
    ArgTypes,
    Canvas,
    Controls,
    Description,
    Meta,
    Source,
    Story,
    Title,
} from "@storybook/blocks";

import { Tooltip } from ".";
import * as stories from "./stories.tsx";

<Meta of={stories} />
<Title of={stories} />
<Description of={Tooltip} />

## Example

<Canvas of={stories.Default} />

## Interactions

### Delay

**Source:**
**[React Aria Documentation](https://react-spectrum.adobe.com/react-aria/Tooltip.html#delay)**

Tooltips appear after a short delay when hovering the trigger, or instantly when
using keyboard focus. This delay is called the "warmup period", and it is a
global timer, shared by all tooltips. Once a tooltip is displayed, other
tooltips display immediately. If the user waits for the "cooldown period" before
hovering another element, the warmup timer restarts.

<Canvas of={stories.Delay} />

### Trigger

**Source:**
**[React Aria Documentation](https://react-spectrum.adobe.com/react-aria/Tooltip.html#trigger)**

By default, tooltips appear both on hover and on focus. The trigger prop can be
set to "focus" to display the tooltip only on focus, and not on hover.

<Canvas of={stories.Trigger} />

## Controlled open state

**Source:**
**[React Aria Documentation](https://react-spectrum.adobe.com/react-aria/Tooltip.html#controlled-open-state)**

The open state of the tooltip can be controlled via the `defaultOpen` and
`isOpen` props.

<Canvas
    of={stories.ControlledOpenState}
    source={stories.ControlledOpenStateTemplate}
/>

## Positioning

The Tooltip's placement with respect to its trigger element can be adjusted
using the `placement` prop.

<Canvas of={stories.Placement} />
<Controls
    of={stories.Placement}
    include={["placement"]}
    meta={stories.tooltipTriggerMeta}
/>

## Props

<ArgTypes of={Tooltip} />
